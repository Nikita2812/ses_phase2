# CSA AIaaS Platform - Python Dependencies
# Sprint 1, 2 & Phase 2 Sprint 1-3: Complete Platform
# Compatible with Python 3.11 - 3.13

# Core Framework
fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0,<0.32.0
pydantic>=2.5.0,<3.0.0
python-dotenv>=1.0.0

# LangChain & LangGraph (updated for compatibility)
langgraph>=0.0.55
langchain>=0.1.0
langchain-core>=0.1.9
langchain-openai>=0.0.5  # Used for OpenRouter API compatibility

# LLM Providers (OpenRouter via OpenAI-compatible API)
# Note: We use langchain-openai with OpenRouter's base_url

# Database
supabase>=2.3.0  # Supabase REST API client
psycopg2-binary>=2.9.9  # PostgreSQL adapter for raw SQL queries (Phase 2 Sprint 2)

# HTTP & Networking
httpx>=0.25.0
requests>=2.31.0

# Data Processing
pydantic-settings>=2.1.0

# Sprint 2: Document Processing & ETL
PyPDF2>=3.0.0  # PDF text extraction
# python-docx>=1.1.0  # Optional: DOCX support (uncomment if needed)
# unstructured>=0.10.0  # Optional: Advanced document processing (production upgrade)

# Testing (for future use)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Phase 2 Sprint 3: Dynamic Execution Engine
networkx>=3.0  # Dependency graph analysis
pyparsing>=3.0.9  # Advanced conditional expression parsing
jsonschema>=4.17.0  # Full JSON Schema validation

# Code Quality (optional - install separately if needed)
# black>=23.12.0
# flake8>=6.1.0
# mypy>=1.7.0

# Production Server (optional)
# gunicorn>=21.2.0
